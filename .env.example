# Google Apps Script web app URL that /api/lead forwards quiz results to.
GOOGLE_SHEETS_WEBHOOK_URL=

# Base URL encoded into the RESULT screen's QR code (detail page / coupon link).
NEXT_PUBLIC_RESULT_BASE_URL=https://example.com/result
